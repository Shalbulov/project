# 🎬 Документация Netflix Data Explorer (FastAPI + PostgreSQL)

Dashboard панель управления для анализа данных Netflix. Приложение позволяет импортировать данные из CSV в базу данных PostgreSQL и предоставляет удобный интерфейс с динамическими фильтрами.

## 🚀 Основные возможности
Автоматический импорт: Скрипт самостоятельно считывает `data/films.csv` и наполняет БД при первом запуске.
Динамическая фильтрация: Выпадающие списки (Год, Страна, Рейтинг, Категория) формируются автоматически на основе данных из БД.
Поиск: Полнотекстовый поиск по названиям фильмов.
Docker-ready: Полная контейнеризация приложения и базы данных.


## 🛠 Технологический стек
Backend: FastAPI (Python 3.12)
Database: PostgreSQL 15
ORM: SQLAlchemy
Frontend: Jinja2 Templates + Bootstrap 5
DevOps: Docker & Docker Compose


## 📋 Предварительные требования
Перед запуском убедитесь, что у вас установлены:
1.  [Docker Desktop](https://www.docker.com/products/docker-desktop/)
2.  [Docker Compose](https://docs.docker.com/compose/install/)


## ⚡️ Быстрый запуск

1.  **Клонируйте репозиторий**:
    git clone <ссылка_на_ваш_репозиторий>
    cd project


2.  **Запустите проект через Docker**:
    docker compose up --build

3.  **Дождитесь уведомления в логах**:
    `INFO: Uvicorn running on http://0.0.0.0:8000`

4.  **Откройте приложение**:
    Перейдите в браузере по адресу: [http://localhost:8000](http://localhost:8000)

## 🔐 Доступ в систему
Для входа в Dashboard используйте стандартные учетные данные:
Логин: `admin`
Пароль: `password123`


## 🏗 Архитектура проекта
`app/main.py` — основная логика FastAPI, обработка маршрутов и фильтрации.
`app/models.py` — описание структуры таблицы `films` для SQLAlchemy.
`app/database.py` — конфигурация подключения к PostgreSQL.
`data/` — директория с исходным файлом `films.csv`. 
`templates/` — HTML шаблоны (Login и Index страницы).



## ⚙️ Технические аспекты

### Устранение проблем (NoneType Error)

В данных Netflix часто встречаются пустые значения (`NULL`). В коде реализована защита:

- В шаблонах Jinja2 используются проверки `{% if film.cast %}`, что предотвращает падение сервера (Error 500) при попытке обработки пустых полей.
- Фильтры в БД используют `DISTINCT` для исключения дубликатов в выпадающих списках.

### Оптимизация
Приложение настроено на лимит в 100 записей на одну страницу для обеспечения высокой скорости загрузки при работе с большими объемами данных (6000+ строк).


## 👨‍💻 Автор Arsen Anarbek
Разработано как демонстрационный проект для Nomad Insurance.